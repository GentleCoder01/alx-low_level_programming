#include "main.h"

/**
 * print_last_digit - print last digit
 * @n: the number
 * Return: value of the last digit
 */
int print_last_digit(int n)
{
	int ld;
	     ld	= n % 10;
	_putchar(id + '0');
	_putchar('\n');
	return (0);
}
